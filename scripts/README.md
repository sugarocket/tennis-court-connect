# 🛠️ Scripts

Utility scripts for development.

## Ideas
- `seed.ts` — Populate mock data (courts, users, events)
- `migrate.ts` — Run DB migrations (if custom backend)
- `generate-types.ts` — Generate TypeScript types from Supabase

## Run
```bash
npx tsx scripts/seed.ts
```
